;; This buffer is for text that is not saved, and for Lisp evaluation.
;; To create a file, visit it with ‘C-x C-f’ and enter text in its buffer.

Cases such as an ellipse expressing a circle, or even a map expressing a piece of land, seem to fit well with this understanding of expression
